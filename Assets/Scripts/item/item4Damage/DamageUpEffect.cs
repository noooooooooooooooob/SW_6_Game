using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamageUpEffect : MonoBehaviour
{
    // Start is called before the first frame update
    public bool isPower;
    public bool isClone;
    public GameObject targetObject;
    public void Effect(){
        if(isPower){
            if(isClone){

            }
            else{

            }
        }
    }
}
